export TF_VAR_do_token=
export AWS_ACCESS_KEY_ID=
export SPACES_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
export SPACES_SECRET_ACCESS_KEY=
export SIGNOZ_ENDPOINT=           # OTLP ingest endpoint (collectors); NOT the API URL Terraform uses
export SIGNOZ_INGESTION_KEY=
export SIGNOZ_ACCESS_TOKEN=       # SigNoz API key (Settings → API Keys, admin) — Terraform alert rules
